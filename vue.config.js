module.exports={
    runtimeCompiler:true,
    // publicPath:'./',
    devServer:{
        //MOCK接口编写的地方
        //每次做更改这个配置文件的时候，都必须重启项目才会生效
        // host: 'localhost',
        // port: 8080,
        // proxy: {
        //
        // }
    }
}